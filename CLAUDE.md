# Claude Notes

Follow `AGENTS.md`. This package is a standalone reusable Game Core library.

Before reporting completion, run:

```bash
npm test
npm run build
```

Do not create remotes automatically.
